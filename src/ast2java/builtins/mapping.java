package builtins;

import builtins.uint.uint256;

import java.util.HashMap;

public class mapping<K, V> extends HashMap<K, V> {

    public void set(K index, V element) {
        this.set(index, element);
    }
}
